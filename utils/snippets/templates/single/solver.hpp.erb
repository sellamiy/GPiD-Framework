#ifndef GPID_<%= @solver.name %>_SOLVER_INLINED_HPP
#define GPID_<%= @solver.name %>_SOLVER_INLINED_HPP

#include <gpid/solver-interfaces/<%= @solver.name %>_outputs.hpp>

<% for @inlined in @solver.solverInlineSnippets %>
#include <gpid/solver-snippets/<%= @solver.name %>/<%= @inlined %>>
<% end %>

#endif
