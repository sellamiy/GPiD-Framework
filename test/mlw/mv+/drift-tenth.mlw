module DriftTenth

 use ieee_float.Float32
 use ref.Ref
 
 let main (unit) : unit
 
 =
 
 let ghost tick = (of_int RNE 1) ./ (of_int RNE 10) in
 let ghost time = ref zeroF in
 
 for i = 0 to 9 do
  invariant { true }
  time := !time .+ tick
 done;
 
 assert { !time <> (of_int RNE 1) }
 
end