module Oopsla24
 use int.Int
 use ref.Ref

 let main(n: int) : unit =

  for i = 0 to n - 1 do
   invariant { true }
   for j = i to n - 1 do
    invariant { true }
    for k = j to n - 1 do
     invariant { true }
     assert { k >= i }
    done
   done
  done

end
