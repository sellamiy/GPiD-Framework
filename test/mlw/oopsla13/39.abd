(size auto)

(lambda eq (l r) (= l r))
(lambda neq (l r) (distinct l r))
(lambda ge (l r) (>= l r))
(lambda gt (l r) (> l r))
(lambda le (l r) (<= l r))
(lambda lt (l r) (< l r))

(lambda minus (l r) (- l r))
(lambda mult (l r) (* l r))
(lambda plus (l r) (+ l r))
(lambda div (l r) (div l r))
(lambda mod (l r) (mod l r))
(lambda uminus (l) (- l))

(declare maxpathlen buf_off bound_off glob3_pathend_off glob3_pathlim_off glob3_dc as int)
(reference buf_off bound_off glob3_pathend_off glob3_pathlim_off glob3_dc)
(declare 0 as int)

(apply mult (over int (strict 1)) int)

(apply eq (over int int) bf symmetric)
(apply neq (over int int) bf symmetric)
(apply le (over int int) bf nonself)
(apply ge (over int int) bf nonself)
(apply lt (over int int) bf nonself)
(apply gt (over int int) bf nonself)

(extend bf)

