module Oopsla03
 use int.Int
 use ref.Ref

 let main (l: int) (n: int) : unit
  requires { 0 < l }
 =
  for k = 1 to n - 1 do
   invariant { true } ();
  done;
  for i = l to n - 1 do
   invariant { true }
   assert { 1 <= i }
  done

end
