module Oopsla30
 use int.Int
 use ref.Ref

 let main(void) : unit diverges =
 let i = ref 0 in let c = ref 0 in
 while !i < 1000 do
  invariant { true }
  c := !c + !i;
  i := !i + 1;
 done;

 assert { !c >= 0 }
end
