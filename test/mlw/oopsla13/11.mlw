module Oopsla11
 use int.Int
 use ref.Ref

 let main (void) : unit =
  let j = ref 0 in
  let i = ref 0 in
  let x = ref 100 in

  while !i < !x do
   invariant { true }
   j := !j + 2;
   i := !i + 1
  done;

  assert { !j = !x * 2 }

end
