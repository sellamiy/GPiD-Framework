(size auto)

(declare x y as int)
(reference x)

(declare 0 99 as bounds)
(declare 0 1 as modres)

(lambda eq (l r) (= l r))
(lambda neq (l r) (distinct l r))
(lambda ge (l r) (>= l r))
(lambda gt (l r) (> l r))
(lambda le (l r) (<= l r))
(lambda lt (l r) (< l r))

(lambda mod2 (l) (mod l 2))

(apply mod2 (over int) modres)

(copy bounds as int)

(apply eq (over modres modres) bf symmetric)

(apply le (over int int) bf nonself)

(apply lt (over int int) bf nonself)

(apply eq (over int int) bf symmetric)
(apply neq (over int int) bf symmetric)


(extend bf)
