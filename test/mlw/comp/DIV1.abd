(size auto)

(lambda eq (l r) (= l r))
(lambda neq (l r) (distinct l r))
(lambda ge (l r) (>= l r))
(lambda gt (l r) (> l r))
(lambda le (l r) (<= l r))
(lambda lt (l r) (< l r))

(lambda plus (l r) (+ l r))
(lambda div (l r) (div l r))
(lambda mod (l r) (mod l r))

(declare a n i r as int)
(reference i r)
(declare 0 as int)

(apply div (over int int) int2)

(apply le (over int int) bf nonself)
(apply lt (over int int) bf nonself)

(apply eq (over int int) bf symmetric)
(apply neq (over int int) bf symmetric)

(apply eq (over int int2) bf)
(apply neq (over int int2) bf)

(extend bf)

